name: X / Twitter PAO Watch (legacy disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy watcher retired
        run: |
          echo "Legacy 5-minute X watcher is disabled."
          echo "The active watcher is .github/workflows/fast-2min-watch.yml."
