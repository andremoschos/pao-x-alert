name: X RSSHub Fallback Diagnostic

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - x_rsshub_diagnostic.py
      - x_rsshub_fallback.py
      - monitor.py
      - panathinaikos_monitor.py
      - .github/workflows/x-rsshub-diagnostic.yml

permissions:
  contents: read

jobs:
  diagnose-rsshub-x:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    env:
      X_AUTH_TOKEN: ${{ secrets.X_AUTH_TOKEN }}
      X_CT0: ${{ secrets.X_CT0 }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install requests
        run: pip install requests

      - name: Verify true authenticated X Latest searches
        run: |
          python - <<'PY'
          import x_rsshub_fallback as x

          combined = {}
          for query in ('Panathinaikos', '#Panathinaikos'):
              tweets = x._fetch_x_authenticated_keyword(query, 40)
              assert tweets, f'{query}: 0 posts'
              authors = {item['author'].lstrip('@').lower() for item in tweets}
              assert len(authors) >= 3, f'{query}: global Latest coverage too narrow: {sorted(authors)}'
              assert all(item['id'].isdigit() and '/status/' in item['url'] for item in tweets)
              for item in tweets:
                  combined[item['id']] = item
              print('X_AUTH_LATEST_SMOKE', {
                  'query': query,
                  'count': len(tweets),
                  'authors': len(authors),
                  'latest': tweets[0]['url'],
              }, flush=True)

          assert combined
          print('X_AUTH_GLOBAL_LATEST_OK', {
              'unique_posts': len(combined),
              'unique_authors': len({item['author'].lower() for item in combined.values()}),
          }, flush=True)
          PY

      - name: Verify production latest aggregation
        run: |
          python - <<'PY'
          import x_rsshub_fallback as x
          tweets = x.fetch_many_keywords(['Panathinaikos', '#Panathinaikos'], 120)
          assert tweets
          authors = {item['author'].lstrip('@').lower() for item in tweets}
          assert len(authors) >= 3
          print('X_PRODUCTION_LATEST_AGGREGATION_OK', {
              'count': len(tweets),
              'authors': len(authors),
              'latest': tweets[0]['url'],
          }, flush=True)
          PY

      - name: Verify emergency timeline recovery
        run: |
          python - <<'PY'
          import x_rsshub_fallback as x
          accounts = [
              'paofc_', 'Paobcgr', 'acpanathinaikos', 'paobc',
              'fmeetsdata', 'Onlypao_gr', 'papanikolaouchs'
          ]
          tweets = x.fetch_users(accounts, 100)
          assert tweets
          assert all(item['id'].isdigit() and '/status/' in item['url'] for item in tweets)
          print('X_TIMELINE_RECOVERY_SMOKE', {'count': len(tweets)})
          PY
