name: X Panathinaikos Realtime V2 (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired watcher
        run: |
          echo "This separate long-running watcher is retired."
          echo "Only Panathinaikos X is now covered by PAO Fast 2-Min Watch with Chromium fallback."
